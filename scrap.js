value = {name: 'Joe', happy:false}
    /**
     * @param values => [{name: 'Joe', happy:false}]
     * Using .filter, return an array of objects where happy equals true
     */
    //  const result = values.filter(mood => mood.happy === true);
    //  return result

    console.log(value.happy)

